{
  "name": "Minimap",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "figplug build -O -o=build",
    "dev": "figplug build -g -w -v -o=build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "figplug": "^0.1.12"
  }
}
